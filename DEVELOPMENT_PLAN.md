**四個階段工作計畫**

不僅是開發流程，也是學習 **AI 編程 (Cursor + Python)** 的實踐路徑：

---

## 📅 PPT 比對工具開發計畫 (AI 編程實踐版)

### 第一階段：穩定基礎與內容解析 (已完成 80%)

**目標：** 確保邏輯對應（Logic Mapping）精準無誤。

- **1.1 環境檢查**：確認 WinPython 的 `python-pptx` 與 `difflib` 運作正常。
    
- **1.2 相似度調優**：測試兩份真實的 PPT。如果標題微調導致配對失敗，調整 `SequenceMatcher` 的閾值（例如從 0.8 調至 0.6）。
    
- **1.3 結構化輸出**：確保 `diff_report.json` 包含：`slide_index`, `status` (Added/Deleted/Modified), `similarity_score`。
    

---

### 第二階段：視覺化轉換 (關鍵突破點)

**目標：** 在不影響 Windows 系統權限下，將投影片轉為圖片。

- **2.1 導入 PyMuPDF (fitz)**：
    
    - **指令建議**：在 Cursor 按 `Ctrl+L` 問：「如何用 PyMuPDF 把 PPTX 的每一頁渲染成高品質的 PNG 圖片？我不想安裝 LibreOffice。」
        
- **2.2 建立快取機制**：在專案下建立 `temp_images/` 資料夾，分別存放 `old` 與 `new` 的圖檔。
    
- **2.3 影像大小對齊**：確保兩份 PPT 的解析度一致，避免因縮放比例不同導致比對錯誤。
    

---

### 第三階段：像素級差異偵測 (Computer Vision)

**目標：** 利用 OpenCV 找出肉眼難以察覺的微調。

- **3.1 影像相減 (Image Subtraction)**：
    
    - **指令建議**：`Ctrl+K` 在 `utils.py` 寫：「使用 OpenCV `absdiff` 比較兩張圖，並產生一張黑白遮罩圖顯示差異處。」
        
- **3.2 輪廓標記 (Contour Detection)**：在差異處畫出**紅色方框**。
    
- **3.3 產出對照圖 (Side-by-Side)**：將「舊圖、新圖、差異標註圖」拼接成一張寬圖，方便快速審閱。
    

---

### 第四階段：報告生成與自動化整合

**目標：** 將零散的 JSON 與圖片整合成可讀報告。

- **4.1 HTML/PDF 報告**：自動產生一個網頁，列出所有有變動的投影片及其對照圖。
    
- **4.2 清理機制**：程式執行完畢後，自動刪除 `temp_images/` 內的暫存檔，保持資料夾乾淨。
    
- **4.3 (進階) CLI 優化**：增加進度條（如 `tqdm`），讓你在處理幾百頁的技術報告時，能看到處理進度。
    

---

